#include<stdio.h>
int main(){
int n ;
printf("enter n: ");
scanf("%d",&n);
for(int i=1;i<=n;i++){  // i is no of rows 
    for(int j=1;j<=i;j++)  // j is no of columns 
    {
        printf("*");
    }printf("\n");
}
        return 0;
}
